import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";
import { Providers } from "./providers";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "ThreadCraft - Premium Clothing",
  description: "Discover premium clothing at ThreadCraft. Shop the latest trends in fashion with easy reservations and AI-powered assistance.",
  keywords: ["clothing", "fashion", "shop", "online store", "threadcraft"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={inter.className}>
        <Providers>{children}</Providers>
      </body>
    </html>
  );
}
